import { Box } from '@mantine/core';

const BreakLine = () => {
  return (
    <Box
      style={{
        width: '100%',
        height:'4px',
        backgroundColor: '#ccc',
        margin: '2rem auto',
      }}
    />
  );
};

export default BreakLine;
